referencefile <- tabPanel(title = "Reference Files", 
                              icon = icon("info-circle")
                              
                              
                              ) # end of file panel